{
  "private": true,
  "dependencies": {
    "passfather": "file:../../.."
  },
  "devDependencies": {
    "ts-node": "10.9.2",
    "typescript": "5.8.3"
  },
  "scripts": {
    "clean": "rm -rf ./node_modules ./index.js",
    "pretest": "npm run clean && npm i",
    "posttest": "npm run clean",
    "test": "tsc index.ts && npx ts-node index.ts && echo '✅ TypeScript integration test passed'"
  }
}

