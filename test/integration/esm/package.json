{
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "passfather": "file:../../.."
  },
  "scripts": {
    "clean": "rm -rf ./node_modules",
    "pretest": "npm run clean && npm i",
    "posttest": "npm run clean",
    "test": "node index.mjs && echo '✅ ESM integration test passed'"
  }
}

